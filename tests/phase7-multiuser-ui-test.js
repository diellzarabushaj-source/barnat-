'use strict';const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8');{const h=read('admin.html'),j=read('admin-dashboard.js'),c=read('admin-dashboard.css');assert.match(h,/data-view="drugs"/);assert.match(h,/data-view="users"/);assert.match(h,/id="adminGate"/);assert.match(h,/id="adminRows"/);assert.match(h,/id="drugRows"/);assert.match(h,/id="refuseDialog"/);assert.match(j,/\/api\/auth\?scope=users/);assert.match(j,/\/api\/clinical-editor/);assert.match(j,/auth\.authUser\?\.role!==['"]admin['"]/);assert.match(j,/email!==OWNER/);assert.match(j,/locked\(['"]Administrimi është i kyçur/);assert.ok(!/auth\.authUser\?\.role!==['"]admin['"][\s\S]{0,180}location\.replace\(['"]\/index/.test(j));assert.match(j,/method:'PATCH'/);assert.match(j,/X-CSRF-Token/);assert.match(j,/scope=verification&document=/);assert.ok(!j.includes('storage/v1'));assert.match(j,/method:'PUT'/);assert.match(j,/method:'POST'/);assert.match(j,/Shënimet dhe recetat private klinike nuk ekspozohen/);for(const t of['--mi-border','--mi-surface','--mi-brand'])assert.ok(c.includes(t));assert.match(c,/\.mi-table-wrap\{[^}]*overflow-x:auto/)}{const v=JSON.parse(read('vercel.json')),r=s=>v.rewrites.find(x=>x.source===s);assert.equal(r('/admin')?.destination,'/admin.html');assert.equal(r('/regjistrimi')?.destination,'/regjistrimi.html');const m=read('middleware.ts');assert.ok(m.includes("'/regjistrimi'"));assert.ok(!/'\/admin'/.test(m));const e=read('admin-entry.js');assert.match(e,/payload\.authUser\?\.role !== 'admin'/);const env=read('.env.example');assert.match(env,/^MEDINDEX_ADMIN_EMAILS=diellzarabushaj@gmail\.com$/m);assert.match(env,/private\.admin_emails\(\)/)}{const h=read('regjistrimi.html'),l=[...h.matchAll(/href="(\/login(?:-v2)?\.html)"/g)].map(m=>m[1]);assert.ok(l.length>=4);assert.ok(l.every(v=>v==='/login-v2.html'));assert.ok(!h.includes('href="/login.html"'))}{const j=read('login.js');assert.match(j,/ACCOUNT_PENDING_APPROVAL/);assert.match(j,/function showPendingApproval/);assert.match(j,/pendingApproval\) return;/)}{const h=read('index.html');assert.match(h,/personal-drugs-ui\.js\?v=/);assert.match(h,/personal-drugs-ui\.css\?v=/);const u=read('personal-drugs-ui.js');assert.match(u,/savePersonalDrug/);assert.match(u,/deletePersonalDrug/);assert.match(u,/Personale · e paverifikuar/);assert.ok(!u.includes('localStorage.setItem'));assert.match(u,/\/api\/clinical-editor/);assert.match(u,/isAdmin = payload\.authUser\?\.role === 'admin'/);assert.match(u,/sharedRow\.hidden = !isAdmin/);assert.ok(!u.includes('cloneNode'));assert.match(u,/createElement\('button'\)[\s\S]{0,200}className = 'pd-launch'/);const c=read('personal-drugs-ui.css');assert.match(c,/\.pd-launch\{[^}]*width:100%/);assert.match(c,/\.pd-launch\{[^}]*box-sizing:border-box/)}{const c=read('user-library-client.js');assert.match(c,/const DRUGS_KEY = /);assert.match(c,/localStorage\.removeItem\(DRUGS_KEY\)/);assert.match(c,/drugs:Object\.entries\(meta\.deletedDrugs\)/);assert.match(c,/deletedDrugs/);assert.ok(c.includes("['medindex:favorites-changed', 'medindex:notes-changed', 'medindex:personal-note-saved', 'medindex:personal-drugs-changed']"))}console.log('Phase 7 multi-user UI contract passed.');