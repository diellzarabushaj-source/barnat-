{
  "schemaVersion": "drx-canonical-substance-export-contract-v1",
  "status": "LIVE_EXPORT_GATE_IMPLEMENTED_WAITING_SUPABASE",
  "publicationAllowed": false,
  "sourceOfTruth": "Supabase canonical substance tables",
  "requiredColumns": [
    "canonical_key",
    "canonical_name"
  ],
  "optionalColumns": [
    "concept_id",
    "synonym_count",
    "product_count"
  ],
  "ordering": [
    "canonical_key ASC",
    "concept_id ASC"
  ],
  "exclusions": [
    "null_or_blank_canonical_key",
    "null_or_blank_canonical_name",
    "duplicate_canonical_key"
  ],
  "exportGate": {
    "requireStableOrdering": true,
    "requireUniqueCanonicalKey": true,
    "requireAtLeast100UncoveredAfterBatch1And2": true,
    "failClosedOnSchemaMismatch": true,
    "requireQueueAdapterParity": true,
    "requireProjectIdMatch": true,
    "requireSupabaseSourceEnvelope": true,
    "requireHashBoundEnvelope": true,
    "rejectDuplicateCanonicalKey": true,
    "requireAllBatch1And2KeysPresent": true,
    "noFallbackCanonicalRows": true
  },
  "intendedQueryShape": "select canonical_key, canonical_name, concept_id from canonical substance view/table ordered deterministically",
  "next": "When Supabase SQL/data access returns, export the exact canonical relation with project/source/timestamp metadata, compute snapshotSha256, then run the production builder. No fallback or stale canonical array is permitted.",
  "queueAdapter": {
    "acceptedDatabaseShape": [
      "canonical_key",
      "canonical_name",
      "concept_id"
    ],
    "acceptedRuntimeShape": [
      "canonicalKey",
      "canonicalName",
      "conceptId"
    ],
    "canonicalKeyNormalization": "exact parity with public.medindex_normalize_substance_term_v1: lowercase + trim + remove every non-[a-z0-9] character",
    "deterministicOrdering": "code-point canonicalKey ASC, then conceptId ASC",
    "maxBatchSize": 500,
    "supportedBatchSizes": [
      100,
      250,
      500
    ],
    "alreadyCoveredAcceptedShape": "objects using canonical_key/canonicalKey or literal canonical-key strings",
    "normalizationSource": "supabase/migrations/20260827130837_p1_clinical_substance_identity_foundation.sql"
  },
  "productionBuilder": "scripts/build-drx-first100-from-canonical-export.js",
  "productionTest": "tests/drx-phase17-canonical-export-gate-test.js"
}
